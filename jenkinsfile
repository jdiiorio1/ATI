// prueba de jenkins file
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello World!"
                sh 'ansible-playbook -i inventory test_ping.yaml'
            }
        }
    }
}
